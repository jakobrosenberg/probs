// import { createDirname, testDir } from "./util.js"
// const __dirname = createDirname(import.meta)

test('parallel tests', () => {
    // const { result, err, ...rest } = testDir(__dirname + '/internal/parallel-tests')
    // console.log({rest})
    // assert.ok(!err, err)
    // console.log(result)
})
