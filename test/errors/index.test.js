// test('setup', (test) => {
//     const { result, err, msg } = testDir('setup', ['--config', '%dirname%/config.js'])
//     result.children['test/setup-file/_tests/setup/setup.test.js'].children
    
//     assert(!err, err)
//     assert(!result.err.length, result.msg)

// })

